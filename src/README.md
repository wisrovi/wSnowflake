# Source Package

Core Python package containing the wSnowflake library modules.

```
src/
└── wSnowflake/        # Main package
    ├── core/         # Core database operations
    ├── builders/     # SQL query builder
    ├── exceptions   # Custom exceptions
    ├── types/       # SQL type mapping
    └── cli/         # CLI tool
```